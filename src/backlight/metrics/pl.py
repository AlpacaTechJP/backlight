import pandas as pd
import numpy as np
from backlight.positions import Positions


def calc_position_performance(
    positions: Positions, start_dt: pd.Timestamp, end_dt: pd.Timestamp
) -> pd.DataFrame:
    return pd.DataFrame()
